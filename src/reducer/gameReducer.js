export const initialGameState = {

}

export const gameReducer = (state, action) => {
    switch(action.type) {
        case 'numberOne':
            return state
        default:
            return state
    }
}